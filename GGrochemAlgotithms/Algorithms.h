#pragma once
#include <iostream>

class Algorithms {
public:
	void BinarySearchFor(const int& nn, const int& edge_in)const;//O(log n)
	void LinearSearchFor(const int& nn, const int& edge_in)const;//O(n)
	int getRndNumberFromTo(const int& lowerLimit, const int& upperLimit )const;
private:

};
